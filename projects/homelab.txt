# One project per file. Same fields as an inline block in info.txt,
# but with no [section] header and no --- separator.
#
# A line that isn't "key = value" continues the field above it, so a
# long description can wrap across as many lines as you like.
#
# This file must be listed under [project_files] in info.txt to appear.

name   = Four-Machine Homelab
status = finished
type   = hybrid
tech   = Linux, Virtualization, Backups, Networking, Cybersecurity

desc   = A self-maintained four-machine homelab consisting of a computer server, twinned NASes with one serving as an encrypted offsite backup, and an AsusWRT router running guest networks and my VPN server among other things.

url    = 
note   = 
